// 042 FU2 — 017 JSON-схемы re-source из kernel @trading-platform/sdk (единый источник).
// Локальные копии (packages/sdk/schemas/017) дропнуты; схемы приходят забандленными внутри kernel.
export type { CoreSchemaName } from '@trading-platform/sdk/validation';
export {
  SCHEMA_FILES,
  SCHEMA_IDS,
  schemaAsset,
  allSchemaAssets,
} from '@trading-platform/sdk/validation';
