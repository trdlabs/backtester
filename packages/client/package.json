{
  "name": "@trading-backtester/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Typed HTTP client for the trading-backtester service. Consumed by trading-lab as a git/path dependency (not published to npm). dist is self-contained — research-contracts types are bundled in, so the consumer needs no workspace resolution.",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "prepack": "tsup"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
